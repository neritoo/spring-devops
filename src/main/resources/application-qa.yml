profile:
  message: Este es el perfil de testing

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_devops?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: userdev
    password: userdev
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
